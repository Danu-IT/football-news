import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;
    gap: 16px;
    cursor: pointer;
    &:hover{
        transition: color 0.5s;
        color: #F5C451;
    }
`